name = "carrossel"
type = "javascript"

# Cloudflare Pages configuration
pages_build_caching_enabled = true

[build]
command = "npm install && npm run build"
cwd = "."
watch_paths = ["**/*.{ts,tsx,js,jsx,json,md}"]

[env.production]
name = "carrossel"
